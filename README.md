# ParseValidateDataInFile
Parse and validate data from a file


Learning Goals
- Take user input and open file name
- Check if fail is valid or invalid
- Parse and validate if the data and reading runs smoothly
- Output data nicely
- Close file
